<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".Fragmento_menu"
    android:orientation="vertical"
    android:layout_gravity="center_horizontal">

        <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:orientation="horizontal"
        android:weightSum="1">

        <Button style="@style/forma"
        android:background="@drawable/icono_linterna"
        android:id="@+id/b_linterna"
        />

        <Space style="@style/espacio"></Space>

        <Button style="@style/forma"
        android:background="@drawable/icono_nivel"
        android:id="@+id/b_nivel"
       />

        <Space style="@style/espacio"></Space>

        <Button style="@style/forma"
        android:background="@drawable/icono_musica"
        android:id="@+id/b_musica"
        />

        <Space style="@style/espacio"></Space>

        <Button style="@style/forma"
        android:background="@drawable/icono_salir"
        android:id="@+id/b_salir"
        />

        </LinearLayout>
</FrameLayout>